---
# Inventario para ambiente de Test
# Este inventario se usa DESPUÉS del bootstrap
# Para el bootstrap inicial, usar hosts-bootstrap.yml
all:
  children:
    test_servers:
      hosts:
        test-server-01:
          ansible_host: 192.168.56.103
          ansible_user: efemaq
          ansible_ssh_private_key_file: ./provision/.ssh/id_ed25519
          ansible_python_interpreter: /usr/bin/python3
      vars:
        env: test
        docker_edition: ce
        docker_users:
          - efemaq
        portainer_version: latest
        portainer_port: 9000
        portainer_port_ssl: 9443
